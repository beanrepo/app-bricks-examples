version: '3'

vars:
  LICENSED_VERSION: '5.0.6'
  REUSE_VERSION: '5.0.2'
  LICENSE_VENV_DIR: '.venv'
  REUSE_VENV_DIR: '.reuse-venv'
  BRICKS_DIR: '../app-bricks-py'

tasks:
  fmt:
    desc: Run all formatters
    cmds:
      - task: fmt:python
      - task: fmt:js

  fmt:python:
    desc: Format Python examples with Ruff lint fixes
    cmds:
      - ruff check --fix --unsafe-fixes

  fmt:js:
    desc: Run ESLint with auto-fix and Prettier on all JS and other files
    cmds:
      - task: init:npm
      - npx eslint --fix '**/*.js'
      - npx prettier --write '**/*.{js,html,css}'

  init:npm:
    desc: Install npm formatter/linter tools locally
    cmds:
      - npm install --no-save --no-package-lock @eslint/js@9.39.1 eslint@9.39.1 eslint-config-prettier@10.1.8 eslint-plugin-prettier@5.5.4 globals@16.5.0 prettier@3.6.2

  license:init-venvs:
    desc: Recreate the environments used by the license tasks
    internal: true
    cmds:
      - task: license:init-licensed-venv
      - task: license:init-reuse-venv

  init:license:
    desc: Backward-compatible alias for recreating the environments used by the license tasks
    cmds:
      - task: license:init-venvs

  init:ci:
    desc: Backward-compatible alias for initializing license environments
    cmds:
      - task: license:init-venvs

  examples:gen-manifest:
    desc: Generate examples.json from the filesystem
    cmds:
      - python3 scripts/examples_manifest.py --write

  examples:check-manifest:
    desc: Fail when examples.json does not match the filesystem
    cmds:
      - python3 scripts/examples_manifest.py --check

  build:
    desc: "Build the release archive in dist/ (task build VERSION=0.13.0rc3): the examples trees, learn-docs, the manifest, requirements.txt and the licensing files (records, texts, REUSE and licensed configuration)"
    requires:
      vars: [VERSION]
    vars:
      DIST_NAME: app-bricks-examples-{{.VERSION}}
    cmds:
      - rm -rf dist
      - mkdir -p dist
      # Tracked files only, from HEAD: no local leftovers (__pycache__, .DS_Store, *.db) end up in the package.
      - git archive --format=tar --prefix=app-bricks-examples/ HEAD core-and-foundational inspirational bricks learn-docs examples.json LICENSE.txt LICENSES REUSE.toml .licensed.yml | tar -x -C dist
      # From the working tree, not HEAD: a pre-release pins them to an app-bricks-py pre-release
      # in the workspace only (task bricks:pin), main keeps the stable pin.
      - cp requirements.txt dist/app-bricks-examples/
      - cp -r .licenses dist/app-bricks-examples/
      - tar -C dist -czf dist/{{.DIST_NAME}}.tar.gz app-bricks-examples
      - cd dist && zip -qr {{.DIST_NAME}}.zip app-bricks-examples
      - cd dist && sha256sum {{.DIST_NAME}}.tar.gz {{.DIST_NAME}}.zip > SHA256SUMS
      - rm -rf dist/app-bricks-examples
      - ls -la dist

  bricks:pin:
    desc: "Pin requirements.txt to an app-bricks-py release and refresh the dependency license records (task bricks:pin -- 0.13.0rc3)"
    cmds:
      - |
        VERSION={{.CLI_ARGS}}
        if [ -z "$VERSION" ]; then
          echo "Usage: task bricks:pin -- <version>"
          exit 1
        fi
        BASE="https://github.com/arduino/app-bricks-py/releases/download"
        WHEEL="arduino_app_bricks-${VERSION}-py3-none-any.whl"
        URL=""
        # bricks/* is the current library release tag prefix, release/* the historical one.
        for PREFIX in bricks release; do
          CANDIDATE="${BASE}/${PREFIX}%2F${VERSION}/${WHEEL}"
          if curl -sfIL -o /dev/null "$CANDIDATE"; then
            URL="$CANDIDATE"
            break
          fi
        done
        if [ -z "$URL" ]; then
          echo "No app-bricks-py release ${VERSION} found (looked for bricks/${VERSION} and release/${VERSION})"
          exit 1
        fi
        sed -i -E "s|^arduino_app_bricks @ .*|arduino_app_bricks @ ${URL}|" requirements.txt
        echo "requirements.txt pinned to ${URL}"
      - task: license:deps

  check:typing:
    desc: "Type-check the examples against the app-bricks-py checkout in {{.BRICKS_DIR}} (pyright, profile api-user), then report the bricks without examples. Extra args go to the run mode, see {{.BRICKS_DIR}}/scripts/check_pyright.py --help"
    cmds:
      - python3 "{{.BRICKS_DIR}}/scripts/check_pyright.py" run --examples-dir . --library-src "{{.BRICKS_DIR}}/src" --python "{{.BRICKS_DIR}}/.venv/bin/python" {{.CLI_ARGS}}
      - python3 "{{.BRICKS_DIR}}/scripts/check_pyright.py" coverage --examples-dir . --head-src "{{.BRICKS_DIR}}/src"

  install-bricks:
    desc: Install a specific bricks version (e.g. task install-bricks -- 0.8.0rc2)
    cmds:
      - |
        VERSION={{.CLI_ARGS}}
        if [ -z "$VERSION" ]; then
          echo "Usage: task install-bricks -- <version>"
          exit 1
        fi
        "{{.LICENSE_VENV_DIR}}"/bin/pip install "https://github.com/arduino/app-bricks-py/releases/download/bricks%2F${VERSION}/arduino_app_bricks-${VERSION}-py3-none-any.whl" \
          || "{{.LICENSE_VENV_DIR}}"/bin/pip install "https://github.com/arduino/app-bricks-py/releases/download/release%2F${VERSION}/arduino_app_bricks-${VERSION}-py3-none-any.whl"

  install:licensed:
    internal: true
    cmds:
      - |
        INSTALLED_VERSION=""
        if command -v licensed >/dev/null 2>&1; then
          INSTALLED_VERSION="$(licensed version 2>/dev/null | grep -Eo '[0-9]+(\.[0-9]+)+' | head -n1)"
        fi

        if [ "$INSTALLED_VERSION" = "{{.LICENSED_VERSION}}" ]; then
          exit 0
        fi

        if [ "${CI:-}" = "true" ]; then
          echo "Installing licensed v{{.LICENSED_VERSION}} for CI..."
          if ! gem install licensed -v {{.LICENSED_VERSION}} --no-document; then
            echo ""
            echo "============================================================"
            echo "  ERROR: licensed v{{.LICENSED_VERSION}} could not be installed in CI."
            echo ""
            echo "  Make sure the workflow provides Ruby and RubyGems correctly,"
            echo "  then retry."
            echo "============================================================"
            exit 1
          fi

          exit 0
        fi

        echo ""
        echo "============================================================"
        echo "  ERROR: licensed v{{.LICENSED_VERSION}} is required to run 'task license:deps'."
        echo ""
        echo "  Install licensed v{{.LICENSED_VERSION}} manually, make sure it is available in PATH,"
        echo "  and then retry."
        echo ""
        echo "  Verify the installation with:"
        echo "    licensed version"
        echo "============================================================"
        exit 1

  license:init-reuse-venv:
    desc: Recreate the dedicated virtualenv used by REUSE
    internal: true
    cmds:
      - |
        rm -rf "{{.REUSE_VENV_DIR}}"
        python3 -m venv "{{.REUSE_VENV_DIR}}"
        "{{.REUSE_VENV_DIR}}"/bin/pip install --upgrade pip
        "{{.REUSE_VENV_DIR}}"/bin/pip install reuse=={{.REUSE_VERSION}}

  license:init-licensed-venv:
    desc: Recreate the dedicated virtualenv used for licensed dependency scanning
    internal: true
    cmds:
      - |
        rm -rf "{{.LICENSE_VENV_DIR}}"
        python3 -m venv "{{.LICENSE_VENV_DIR}}"
        "{{.LICENSE_VENV_DIR}}"/bin/pip install --upgrade pip -q

        failed_pkgs=()
        while IFS= read -r line || [ -n "$line" ]; do
          pkg=$(echo "$line" | sed 's/#.*//;s/^[[:space:]]*//;s/[[:space:]]*$//')
          [ -z "$pkg" ] && continue
          "{{.LICENSE_VENV_DIR}}"/bin/pip install -q "$pkg" || failed_pkgs+=("$pkg")
        done < requirements.txt

        if [ ${#failed_pkgs[@]} -gt 0 ]; then
          for pkg in "${failed_pkgs[@]}"; do
            echo "::warning::Failed to install requirement [$pkg] in {{.LICENSE_VENV_DIR}}" >&2
          done
        fi

  license:cleanup-reuse-venv:
    desc: Remove the dedicated REUSE virtualenv
    internal: true
    cmds:
      - rm -rf "{{.REUSE_VENV_DIR}}"

  license:cleanup-licensed-venv:
    desc: Remove the dedicated licensed virtualenv
    internal: true
    cmds:
      - rm -rf "{{.LICENSE_VENV_DIR}}"

  license:cleanup-venvs:
    desc: Remove the dedicated license-checking virtualenvs
    internal: true
    cmds:
      - task: license:cleanup-reuse-venv
      - task: license:cleanup-licensed-venv

  license:headers:
    desc: |
      Update license headers and validate REUSE compliance.
      Uses a dedicated virtualenv for REUSE and cleans it up afterwards.
    cmds:
      - task: license:init-reuse-venv
      - |
        cleanup() {
          rm -rf "{{.REUSE_VENV_DIR}}"
        }
        trap cleanup EXIT

        status=0

        if [ "$status" -eq 0 ]; then
          # Annotate first-party textual sources without touching vendored
          # libraries, third-party fonts, or binary/media assets that would
          # otherwise generate .license sidecars.
          find inspirational \
            \( -path '*/python/*' -o -path '*/assets/*' -o -path '*/sketch/*' \) \
            -type f \
            ! -path '*/assets/libs/*' \
            ! -path '*/assets/fonts/*' \
            \( -name '*.py' -o -name '*.js' -o -name '*.css' -o -name '*.html' -o -name '*.ino' -o -name '*.h' \) \
            -exec "{{.REUSE_VENV_DIR}}"/bin/reuse annotate \
              --skip-unrecognized \
              --merge-copyrights \
              --copyright-prefix 'spdx-string-c' \
              --copyright 'Arduino s.r.l. and/or its affiliated companies' \
              --exclude-year \
              --license 'MPL-2.0' \
              '{}' \
              ';' || status=$?
        fi

        if [ "$status" -eq 0 ] && [ -d core-and-foundational ]; then
          find core-and-foundational \
            \( -path '*/python/*' -o -path '*/assets/*' -o -path '*/sketch/*' \) \
            -type f \
            ! -path '*/assets/libs/*' \
            ! -path '*/assets/fonts/*' \
            \( -name '*.py' -o -name '*.js' -o -name '*.css' -o -name '*.html' -o -name '*.ino' -o -name '*.h' \) \
            -exec "{{.REUSE_VENV_DIR}}"/bin/reuse annotate \
              --skip-unrecognized \
              --merge-copyrights \
              --copyright-prefix 'spdx-string-c' \
              --copyright 'Arduino s.r.l. and/or its affiliated companies' \
              --exclude-year \
              --license 'MPL-2.0' \
              '{}' \
              ';' || status=$?
        fi

        if [ "$status" -eq 0 ] && [ -d bricks ]; then
          find bricks \
            \( -path '*/python/*' -o -path '*/assets/*' -o -path '*/sketch/*' \) \
            -type f \
            ! -path '*/assets/libs/*' \
            ! -path '*/assets/fonts/*' \
            \( -name '*.py' -o -name '*.js' -o -name '*.css' -o -name '*.html' -o -name '*.ino' -o -name '*.h' \) \
            -exec "{{.REUSE_VENV_DIR}}"/bin/reuse annotate \
              --skip-unrecognized \
              --merge-copyrights \
              --copyright-prefix 'spdx-string-c' \
              --copyright 'Arduino s.r.l. and/or its affiliated companies' \
              --exclude-year \
              --license 'MPL-2.0' \
              '{}' \
              ';' || status=$?
        fi

        if [ "$status" -eq 0 ]; then
          "{{.REUSE_VENV_DIR}}"/bin/reuse download --all || status=$?
        fi

        if [ "$status" -eq 0 ]; then
          "{{.REUSE_VENV_DIR}}"/bin/reuse lint || status=$?
        fi

        exit "$status"

  license:deps:
    desc: |
      Check and cache dependency licenses with licensed.
      Uses a dedicated virtualenv for pip dependency scanning and keeps it after the task completes.
      Requires licensed v{{.LICENSED_VERSION}} for dependency license checking.
      In local environments, install licensed v{{.LICENSED_VERSION}} manually before running this task.
      In CI, the task installs licensed v{{.LICENSED_VERSION}} with gem after Ruby has been set up by the workflow.
    cmds:
      - task: install:licensed
      - task: license:init-licensed-venv
      - |
        status=0

        if [ "$status" -eq 0 ]; then
          licensed cache || status=$?
        fi

        if [ "$status" -eq 0 ]; then
          licensed status || status=$?
        fi

        exit "$status"

  license:
    desc: Update license headers, validate REUSE compliance and check dependency licenses
    cmds:
      - task: license:headers
      - task: license:deps
